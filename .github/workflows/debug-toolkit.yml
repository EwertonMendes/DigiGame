name: Developer Toolkit CI Exclusion

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  excluded:
    name: Developer toolkit is intentionally not regression-tested
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Developer Toolkit is development/debug-only and intentionally excluded from CI regression testing."
